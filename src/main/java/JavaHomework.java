import java.util.Arrays;
import java.util.Collection;

public class JavaHomework {
    public static void main(String[] args) {
/*

       int random = (int)(Math.random() * 101 + 0);
        System.out.println(random);
*/
/*
String[] letterGrade = {"A", "B", "C", "D", "F"};
        System.out.println(Arrays.toString(letterGrade));
*/

/*
        char letterGrade;
        int score = 90;
        if (score >= 90)
            letterGrade = 'A';
        else if (score >= 80)
            letterGrade = 'B';
        else if (score >= 70)
            letterGrade = 'C';
        else if (score >= 60)
            letterGrade = 'D';
        else
            letterGrade = 'F';
        System.out.println("Score: " + score + " Grade: " + letterGrade);
*/


/*
        String fullName = "Mike Jones";
        int start = fullName.indexOf(' ');
        int end = fullName.lastIndexOf(' ');
        String firstName = "Mike";
        String lastName = "Johns";
        if (start >= 0) {
            firstName = fullName.substring(0, start);
            if (end > start)
                fullName = fullName.substring(start + 1, end);
            lastName = fullName.substring(end + 1, fullName.length());
        }
        System.out.println(fullName);
        System.out.println(firstName);
        System.out.println(lastName);
*/
/*
        int luckyNumber = (int)(Math.random() *  4 + 0);
        System.out.println(luckyNumber);
*/

/*
        int luckyNumber = (int)(Math.random() *  4 + 0);
        String numberString;

        switch (luckyNumber)
        {
            case 1:  numberString = "0";
                break;
            case 2:  numberString ="1";
                break;
            case 3:  numberString ="2";
                break;
                default: numberString = "3";
        }
        System.out.println("Your lucky number is " + numberString);
*/

    }
        }

